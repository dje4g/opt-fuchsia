# flex package spec

declare -r PKG_NAME=flex
declare -r PKG_VERSION=2.5.39
declare -r PKG_TARBALL=${PKG_NAME}-${PKG_VERISON}.tar.xz
declare -r PKG_URL=$OPT_URL_GNU/$PKG_NAME/$PKG_TARBALL
declare -r PKG_PATCHES=${PKG_NAME}-${PKG_VERISON}.patches
