# missing-libc package spec
# This package supplies misc things from glibc to simplify porting.

declare -r PKG_NAME=missing-libc-0.1
declare -r PKG_TARBALL=${PKG_NAME}.tar.gz
declare -r PKG_SRC=${PKG_NAME}
