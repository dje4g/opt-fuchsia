# pkg-config package spec

declare -r PKG_NAME=pkg-config
declare -r PKG_VERSION=0.28
declare -r PKG_TARBALL=${PKG_NAME}-${PKG_VERSION}.tar.gz
declare -r PKG_URL=http://pkgconfig.freedesktop.org/releases/pkg-config-0.28.tar.gz
declare -r PKG_SIG=md5sum:aa3c86e67551adc3ac865160e34a2a0d
declare -r PKG_PATCHES=${PKG_NAME}-${PKG_VERSION}.patches
